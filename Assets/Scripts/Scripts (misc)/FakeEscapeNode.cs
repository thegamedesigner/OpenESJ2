using UnityEngine;
using System.Collections;

public class FakeEscapeNode : MonoBehaviour
{
    //Checks if the player is holding down ESC or just tapping it. Resets the controls if they're holding it.

    public TextMesh textMesh;
    public TextMesh textMesh2;
    float timeSet = 0;
    bool resetControls = false;

    void Update()
    {
		
    }
}
