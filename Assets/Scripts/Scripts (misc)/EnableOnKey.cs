using UnityEngine;
using System.Collections;

public class EnableOnKey : MonoBehaviour
{/*
    public GC_CustomControls.Action key = GC_CustomControls.Action.USE;
    public Behaviour enableThis = null;
    void Update()
    {
        if (Controls.GetKeyDown(Controls.ConvertFromGCControls(key), 0))//(GC_CustomControls.GetKeyDown(key))
        {
            enableThis.enabled = true;
            this.enabled = false;
        }
    }*/
}
